#include <stdio.h>
#include <cs50.h>

int main(void)
{
    int n;
    do
    {
        n = get_int("Pyramid Height: ");
    }
    while (n < 1 || n > 8);

    for (int i = 1; i <= n -1 || i >; i++)
    {
        printf(
        );
    }
    printf("\n");
}
